# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Default 5001; if busy the server tries the next ports and writes backend/.dev-port for Vite.
PORT=5001
